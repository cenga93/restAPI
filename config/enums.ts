export enum TokenTypes {
     ACCESS = 'access',
     REFRESH = 'refresh',
     RESET_PASSWORD = 'resetPassword',
}

export enum MembershipTypes {
     TRIAL = 'trial',
     FREE = 'free',
     PAID = 'paid',
}
